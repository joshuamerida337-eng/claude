{
  "name": "ai-free-for-boomers",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
